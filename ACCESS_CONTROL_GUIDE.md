# 🔐 访问控制系统使用指南

## 功能概述

让台湾（或其他地方）的团队成员访问你的Streamlit监控系统，**无需修改路由器设置**。

### 工作流程

```
新访客访问 → 填写申请表单 → 管理员收到通知 → 审批通过 → 访客可以查看数据
```

---

## 🎯 用户视角

### 第一次访问（自动成为管理员）

- 第一个访问系统的人自动获得管理员权限
- 右上角显示"👑 管理员"标记
- 可以审批其他人的访问请求

### 新用户申请访问

1. 打开你提供的Streamlit URL（例如 `http://你的IP:8501`）
2. 看到申请表单：
   ```
   📝 申请访问权限
   ━━━━━━━━━━━━━━━
   姓名: [李华          ]
   理由: [我是台湾团队成员...]
   
   [提交申请]
   ```
3. 填写后提交，页面显示"⏳ 等待审批..."
4. 等待管理员批准后刷新页面

### 管理员审批请求

1. 右上角显示 **🔔1** 红色数字（有1个待审批）
2. 点击数字，弹出审批面板：
   ```
   📋 待审批请求
   
   🆕 李华 (123.45.67.89)
   理由: 我是台湾团队成员...
   时间: 2026-10-10 15:30
   
   [✅ 批准]  [❌ 拒绝]
   ```
3. 点击"✅ 批准"，对方立即可以访问

---

## 🛠️ 技术实现

### 文件结构

```
BGC_2026_Production/
├── access_control.py          # 核心逻辑
├── data/
│   └── access_requests.json   # 存储访问记录
├── app.py                     # 集成访问控制
└── test_access_ui.py          # 测试界面
```

### 数据存储格式

**data/access_requests.json**:
```json
{
  "192.168.1.100": {
    "ip": "192.168.1.100",
    "status": "granted",
    "role": "admin",
    "granted_at": "2026-10-10T10:00:00"
  },
  "123.45.67.89": {
    "ip": "123.45.67.89",
    "name": "李华",
    "reason": "台湾团队成员",
    "status": "pending",
    "requested_at": "2026-10-10T15:30:00"
  }
}
```

### 角色权限

| 角色 | 权限 |
|------|------|
| **admin** | 查看所有数据 + 审批访问 + 配置系统 |
| **viewer** | 仅查看数据（无法修改配置） |

---

## 🧪 测试流程

### 1. 启动测试界面

```bash
streamlit run test_access_ui.py --server.port 8502
```

访问：http://localhost:8502

### 2. 模拟多用户

左侧切换不同身份：
- **管理员 (192.168.1.100)** - 第一个访客
- **台湾访客 (123.45.67.89)** - 需要申请
- **其他访客 (111.222.333.444)** - 需要申请

### 3. 完整测试场景

**场景A：首次访问（管理员）**
```
1. 选择"管理员 (192.168.1.100)"
2. 看到：✅ GRANTED / 👑 ADMIN
3. 显示"🎉 你是管理员！"
```

**场景B：新用户申请**
```
1. 选择"台湾访客 (123.45.67.89)"
2. 看到：🆕 NEW
3. 填写表单：
   姓名: 李华
   理由: 我是台湾团队成员，需要查看实时监控
4. 提交 → 状态变为 ⏳ PENDING
```

**场景C：管理员审批**
```
1. 切换回"管理员"
2. 看到：待审批请求 🔔 1
3. 点击查看详情
4. 批准 → 台湾访客状态变为 ✅ GRANTED / 👁️ VIEWER
```

---

## 📱 实际部署（让台湾朋友访问）

### 你需要做的：

1. **确保Streamlit已启动**
   ```bash
   streamlit run app.py --server.address 0.0.0.0 --server.port 8501
   ```

2. **获取你的公网IP**
   ```bash
   curl ifconfig.me
   ```
   假设得到：`203.123.45.67`

3. **设置路由器端口转发（可选）**
   - 如果你在路由器后面，需要转发8501端口
   - 或者使用Ngrok：`ngrok http 8501`

4. **把URL发给台湾朋友**
   ```
   http://203.123.45.67:8501
   ```

### 台湾朋友需要做的：

1. 打开你提供的URL
2. 看到申请表单，填写姓名和理由
3. 提交后等待你批准

### 你审批流程：

1. 你的网站右上角显示 🔔1
2. 点击查看详情：
   ```
   姓名: 林志明
   IP: 台湾某IP
   理由: 需要查看实时监控数据
   ```
3. 点击"✅ 批准"
4. 对方刷新页面即可进入

---

## ⚠️ 注意事项

### 安全性

- ✅ **第一个访客自动成为管理员**，确保你自己先访问
- ✅ 每个IP只能提交一次申请
- ✅ 管理员可以拒绝任何请求
- ⚠️ IP地址可以被伪造（如果需要更高安全性，考虑添加密码）

### 局限性

- **动态IP问题**：如果你的公网IP会变，需要重新告知对方
- **路由器限制**：某些ISP不允许端口转发
- **防火墙**：确保Windows防火墙允许端口8501

### 替代方案（如果端口转发失败）

**使用Ngrok（推荐）**：
```bash
ngrok http 8501
```
会给你一个固定URL，例如 `https://abc123.ngrok.io`

优点：
- 无需改路由器
- 自动HTTPS加密
- 访问控制仍然有效

缺点：
- 免费版每次重启URL会变
- 速度较慢

---

## 🔍 故障排查

### 问题1：右上角没有显示管理员面板

**原因**：你不是第一个访客，或者数据文件被删除

**解决**：
```bash
# 删除访问记录，重新开始
rm data/access_requests.json

# 重启Streamlit
streamlit run app.py
```

### 问题2：对方无法访问URL

**检查清单**：
- [ ] Streamlit是否启动（`--server.address 0.0.0.0`）
- [ ] 路由器是否设置端口转发
- [ ] Windows防火墙是否允许8501端口
- [ ] 公网IP是否正确（`curl ifconfig.me`）

### 问题3：批准后对方仍然看到"等待审批"

**解决**：让对方刷新页面（Ctrl+F5 强制刷新）

---

## 📊 监控访问记录

查看所有访问请求：
```python
from access_control import get_all_requests

requests = get_all_requests()
for ip, data in requests.items():
    print(f"{ip}: {data['status']} - {data.get('name', 'N/A')}")
```

查看待审批数量：
```python
from access_control import get_pending_count

pending = get_pending_count()
print(f"待审批: {pending}")
```

---

## ✅ 系统已集成到

- ✅ **TEST模式** (app.py line ~511)
- ✅ **LIVE模式** (app.py line ~762)
- ✅ 右上角管理员面板
- ✅ 待审批红色数字通知
- ✅ 一键批准/拒绝功能

---

## 🎉 完成！

现在你可以：
1. 不用改任何网络配置（如果用Ngrok）
2. 台湾朋友填表申请访问
3. 你在网站上一键批准
4. 对方立即可以查看监控数据

**测试地址**：http://localhost:8502
**实际地址**：http://localhost:8501（集成到主系统）
