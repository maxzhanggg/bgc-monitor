# WiFi本地访问 + 外网申请控制

## 当前配置

### ✅ 自动通过（无需申请）
- **本机 127.0.0.1** → 永久管理员
- **同WiFi下的设备**（192.168.x.x）→ 直接访问

### 🔐 需要申请（外网IP）
- 台湾、香港、其他城市等不同网络的访问者
- 他们访问时会看到申请表单
- 你的右上角显示 🔔 + 数字通知
- 一键批准后他们可进入

---

## 使用方法

### 1️⃣ 启动系统
```bash
cd C:\Users\zhang\Desktop\BGC_2026_Production
streamlit run app.py --server.address 0.0.0.0
```

### 2️⃣ 本机访问（你）
打开浏览器访问：`http://localhost:8501`
- 自动识别为管理员
- 右上角可以看到审批面板

### 3️⃣ 同WiFi设备访问（同事/测试机）
查询你的本机IP：
```bash
ipconfig
# 找到 IPv4 地址，例如 192.168.1.100
```

其他设备访问：`http://192.168.1.100:8501`
- 同WiFi设备直接进入，无需申请

### 4️⃣ 外网访问（台湾团队）
**方式A：端口转发**
1. 路由器设置端口转发：`外网端口8501 → 你的内网IP:8501`
2. 查询你的公网IP：访问 `https://ip.cn`
3. 发给台湾朋友：`http://你的公网IP:8501`

**方式B：Ngrok（推荐）**
```bash
# 下载 ngrok: https://ngrok.com/download
ngrok http 8501

# 会生成一个公网URL，例如：
# https://abc123.ngrok.io
```
把这个URL发给台湾朋友即可

### 5️⃣ 审批流程
1. 台湾朋友访问URL → 看到申请表单
2. 填写姓名和理由 → 提交
3. 你的网站右上角显示 🔔1
4. 点击展开 → 点击 ✅ 批准
5. 对方刷新页面 → 进入系统

---

## 技术说明

### IP识别逻辑
```python
# 本机 → 管理员
127.0.0.1, localhost, ::1

# 同WiFi → 直接通过（白名单）
192.168.x.x

# 其他IP → 需申请
123.45.67.89（台湾）
```

### 数据存储
所有访问记录保存在：
```
data/access_control.json
```

### 角色权限
- **admin**: 可以审批、可以查看所有数据
- **viewer**: 只能查看数据，不能审批

---

## 测试验证

当前白名单中已有的IP：
- `127.0.0.1` - 本机管理员 ✅
- `192.168.1.100` - 测试管理员（旧）
- `192.168.1.101` - 测试访客（旧）

现在直接访问 http://localhost:8501 即可进入管理面板。
